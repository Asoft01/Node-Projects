npm install axios 
npm init -y 
npm audit 
npm audit fix
npm install nodemon 
npm install -g nodemon 
in package.json, try and add 
"scripts": {
    "start": "node request.js",
    "dev" : "nodemon request.js", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },

uncommit git 
git reset --hard 
git clean -fxd